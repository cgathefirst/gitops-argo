---
# Source: just-app/templates/service.yaml
kind: Service     
apiVersion: v1     
metadata:
  name: examgabieyal-4-svc #from RELEASE NAME
spec:
  type: ClusterIP
  ports:
   - port: 8000  
     targetPort: 8000  
     name: http   
     protocol: TCP
  selector:
    app: examgabieyal-4
---
# Source: just-app/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: examgabieyal-4-deploy
spec:
  selector: 
    matchLabels:
      app: examgabieyal-4
  replicas: 1   #from values
  template:
    metadata:
      labels:
        app: examgabieyal-4
    spec:
      containers:
      - name: examgabieyal-4 #from values
        image: cgabya12/examgabieyal:latest #from values
        ports:
        - containerPort: 8000
---
# Source: just-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name:  examgabieyal-4-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name:  examgabieyal-4-svc
                port:
                  number: 8000
