---
# Source: just-app/templates/service.yaml
kind: Service     
apiVersion: v1     
metadata:
  name: examgabieyal-prod-svc #from RELEASE NAME
spec:
  type: ClusterIP
  ports:
   - port: 8000  
     targetPort: 8000  
     name: http   
     protocol: TCP
  selector:
    app: examgabieyal-prod
---
# Source: just-app/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: examgabieyal-prod-deploy
spec:
  selector: 
    matchLabels:
      app: examgabieyal-prod
  replicas: 1   #from values
  template:
    metadata:
      labels:
        app: examgabieyal-prod
    spec:
      containers:
      - name: examgabieyal-prod #from values
        image: cgabya12/examgabieyal:prod #from values
        ports:
        - containerPort: 8000
---
# Source: just-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name:  examgabieyal-prod-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name:  examgabieyal-prod-svc
                port:
                  number: 8000
